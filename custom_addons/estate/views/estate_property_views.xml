<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_estate_property_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Emlak Mülkleri">
                <field name="name"/>
                <field name="bedrooms"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="availability_date" optional="hide"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Emlak Mülkü">
                <header>
                    <button name="action_sold" type="object" string="Sold" invisible="state in ('sold', 'canceled')"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="state in ('sold', 'canceled')"/>
                    <field name="state" widget="statusbar" 
                           statusbar_visible="new,offer_received,offer_refused,offer_accepted,sold"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Örn: Boğaz Manzaralı Villa"/>
                        </h1>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color', 'no_create': True}"/>
                    </div>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="availability_date"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                        <group string="Konum/Bahçe">
                            <field name="garden"/>
                            <field name="garden_area" invisible="not garden"/>
                            <field name="garden_orientation" invisible="not garden"/>
                        </group>
                        <group string="Kişiler">
                            <field name="salesperson_id"/>
                            <field name="buyer_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Teklifler">
                            <field name="offer_ids" readonly="state in ('offer_accepted', 'sold', 'canceled')">
                                <tree editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="partner_id"/>
                                    <field name="price"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status" column_invisible="1"/>
                                    <button name="action_accept" type="object" string="Kabul Et" icon="fa-check" invisible="status in ('accepted', 'refused')"/>
                                    <button name="action_refuse" type="object" string="Reddet" icon="fa-times" invisible="status in ('accepted', 'refused')"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>
    
    <!-- Search View -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Mülk Ara">
                <field name="name" string="Mülk Adı veya Türü" filter_domain="['|', ('name', 'ilike', self), ('property_type_id', 'ilike', self)]"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="expected_price"/>
                <separator/>
                <filter string="Müsait" name="available" domain="[('state', 'in', ('new', 'offer_received','offer_refused' ))]"/>
                <group expand="1" string="Group By">
                    <filter string="Durum" name="state" context="{'group_by':'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="view_estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="0">
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click oe_kanban_card">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="name"/>
                                    </strong>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <div>
                                    <span>Beklenen Fiyat: </span>
                                    <field name="expected_price"/>
                                </div>
                                <!-- Sadece Teklif Alındığında (offer_received) -->
                                <div t-if="record.state.raw_value == 'offer_received'">
                                    <span>En İyi Teklif: </span>
                                    <field name="best_price"/>
                                </div>
                                <!-- Sadece Kabul Edildiğinde (offer_accepted) -->
                                <div t-if="record.state.raw_value == 'offer_accepted'">
                                    <span>Satış Fiyatı: </span>
                                    <field name="selling_price"/>
                                </div>
                            </div>
                            <!-- Alt kısma etiketleri ekleyelim -->
                            <div class="o_kanban_record_bottom mt-2">
                                <div class="oe_kanban_bottom_left">
                                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Action -->
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Mülkler</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="context">{'search_default_available': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Mülk eklemek için "New" butonuna tıklayın.
            </p>
        </field>
    </record>
</odoo>
